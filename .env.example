# BetterFlow Sync environment overrides
#
# Agent API endpoint used for data sync
# BETTERFLOW_API_URL=http://127.0.0.1:8001/api/agent
#
# Optional web app base URL used for browser auth/dashboard links.
# Set this when API is local but auth should still use production UI.
# BETTERFLOW_WEB_BASE_URL=https://app.betterflow.eu
#
# Optional explicit .env file path (advanced)
# BETTERFLOW_SYNC_ENV_FILE=/absolute/path/to/.env
